const square = (x) => {
    return x*x;
}

const square = num => {
    return num*num;
}

const add = (x, y) => {
    return x + y;
}

const rollDie = () => {
    return Math.floor(Math.random() * 6 ) + 1;
}